apiVersion: v1
kind: Service
metadata:
  name: nvidia-com-in
  namespace: blue
  labels:
    app: nvidia
spec:
  ports:
  - port: 80
    name: nvidia-inside
  selector:
    app: nvidia
---
apiVersion: v1
kind: Service
metadata:
  name: nvidia-com-out
  namespace: blue
  labels:
    app: nvidia
spec:
  ports:
  - port: 8002
    name: nvidia-outside
  selector:
    app: nvidia
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: nvidia
  namespace: blue
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: nvidia
        version: v1
        cluster: "sjc"
        role: minion
    spec:
      containers:
      - name: minion
        image: davigupta/minion:0.60
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        - containerPort: 8002
        env:
        - name: MY_NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: MY_POD_NAME
          value: "nvidia-com"
        - name: MY_POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: MY_POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: MY_POD_CLUSTER
          valueFrom:
            fieldRef:
              fieldPath: metadata.labels['cluster']
        - name: MY_DNS
          value: "10.110.209.4"
      terminationGracePeriodSeconds: 60
      imagePullSecrets:
      - name: regcred
---
