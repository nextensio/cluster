apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: node2-int
spec:
  hosts:
  - k8s-worker2.node.consul
  ports:
  - number: 8500
    name: consul
    protocol: HTTP
  resolution: NONE
  location: MESH_INTERNAL
